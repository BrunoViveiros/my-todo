import { TodoList } from "../../modules";

import { Container } from "./Homepage.styles";

const Homepage = () => {
  return (
    <Container>
      <TodoList />
    </Container>
  );
};

export default Homepage;
